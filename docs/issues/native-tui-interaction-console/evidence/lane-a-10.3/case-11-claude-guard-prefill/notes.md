empty composer, styled capture (placeholder = dim/inverse cells): 01-empty-styled.txt
prefill after one Escape: SURVIVED (styled captures 03 vs 05; the r5 evidence says Claude prefill survives Escape).
PASS: declared /compact refused composer-nonempty; styled capture byte-identical before/after (prefill untouched, zero command bytes).
empty composer, styled capture (placeholder = dim/inverse cells): 01-empty-styled.txt
prefill after one Escape: SURVIVED (styled captures 03 vs 05; the r5 evidence says Claude prefill survives Escape).
PASS: declared /compact refused composer-nonempty; styled capture byte-identical before/after (prefill untouched, zero command bytes).
