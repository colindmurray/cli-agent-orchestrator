model key present in ~/.claude.json before drill: False
control evidence claim: bytes delivered (accepted, per-event sent) — a queued command; no menu execution or cancellation is claimed anywhere
model/effort menu opened during the active turn: False (the pinned limit says it must not)
post-turn the queued /model opened the selector (ordinary queued-input processing after the turn, not a mid-turn menu); Escape-closed idle
model key present in ~/.claude.json after drill: False
