fused sequence outcome: NO model-switch status line after the fused batch (picker mounted=True, still open=True). The Up/Enter keys of the fused form arrive before the picker finishes mounting and do not commit a selection on this build — the §10.3 fused form is not a deterministic model change here; the paced form is proven in case-04.
precondition menu-close Escape #1: outcome=accepted
fused sequence outcome: NO model-switch status line after the fused batch (picker mounted=True, still open=True). The Up/Enter keys of the fused form arrive before the picker finishes mounting and do not commit a selection on this build — the §10.3 fused form is not a deterministic model change here; the paced form is proven in case-04.
precondition menu-close Escape #1: outcome=accepted
