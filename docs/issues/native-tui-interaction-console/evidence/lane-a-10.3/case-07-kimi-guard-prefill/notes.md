prefill after one Escape: SURVIVED (compare 02 vs 04).
guard refusal detail: "the composer's emptiness could not be proven (the input region was unreadable or unparseable), and a declared command is written only against a proven-empty composer; zero bytes were written"
PASS: declared /compact refused composer-nonempty; prefill byte-identical (' queued draft'); zero command bytes (no compaction UI in transcript).
prefill after one Escape: SURVIVED (compare 02 vs 04).
guard refusal detail: 'the composer holds content; a declared command submitted now would concatenate with the queued prefill and deliver as ordinary prompt text. Zero command bytes were written and the prefill is untouched — submit or clear it as the operator, then retry the command'
PASS: declared /compact refused composer-nonempty; prefill byte-identical (' queued draft'); zero command bytes (no compaction UI in transcript).
